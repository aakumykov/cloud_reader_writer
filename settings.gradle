pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        mavenLocal()
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "cloud_reader_writer"
include ':app'
include ':cloud_writer'
include ':file_lister_navigator_selector'

project(":file_lister_navigator_selector").projectDir = new File("../file_lister_navigator_selector/lib")